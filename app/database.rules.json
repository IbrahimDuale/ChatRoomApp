{
  "rules": {
    "rooms": {
      "$roomName": {
        "members": {
          ".read": "auth !== null",
          ".write": "auth !== null && auth.uid == $userId",
          "$memberId": {}
        }
      }
    }
  }
}